sudo: false
language: php
os:
  - linux
php:
  - 7.3.1
  - nightly
matrix:
  allow_failures:
    - php: nightly
before_script:
  - composer self-update
  - composer install --prefer-dist
script: vendor/bin/phpunit
notifications:
  email: false
